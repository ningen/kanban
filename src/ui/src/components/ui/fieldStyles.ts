/** Shared style string for form fields (input/select/textarea). */
export const fieldControl =
  "w-full rounded-md border border-border bg-surface-2 px-3 py-2 text-sm text-text " +
  "placeholder:text-text-faint focus:border-accent focus:outline-none " +
  "focus:ring-2 focus:ring-accent/30 disabled:opacity-50";

/** Label styling for form field labels. */
export const fieldLabel = "text-xs font-medium text-text-dim";
